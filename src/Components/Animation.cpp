#include "./Animation.h"

Animation::Animation(unsigned int index, unsigned int numFrames, unsigned int animationSpeed)
    : m_index(index), m_numFrames(numFrames), m_animationSpeed(animationSpeed)
{}